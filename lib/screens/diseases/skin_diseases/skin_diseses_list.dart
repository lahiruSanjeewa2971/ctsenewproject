import 'package:flutter/material.dart';

class SkinDiseases extends StatelessWidget {
  const SkinDiseases({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
