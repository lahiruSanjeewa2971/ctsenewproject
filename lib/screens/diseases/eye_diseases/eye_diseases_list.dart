import 'package:flutter/material.dart';

class EyeDiseasesList extends StatelessWidget {
  const EyeDiseasesList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
