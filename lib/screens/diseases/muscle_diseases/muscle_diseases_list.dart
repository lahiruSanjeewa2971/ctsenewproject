import 'package:flutter/material.dart';

class MuscleDiseases extends StatelessWidget {
  const MuscleDiseases({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
